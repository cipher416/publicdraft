{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "bun install --frozen-lockfile && bun run build:server",
    "watchPatterns": [
      "src/**",
      "package.json",
      "bun.lock",
      "tsconfig*.json",
      "bunfig.toml"
    ]
  },
  "deploy": {
    "startCommand": "bun run start:web",
    "healthcheckPath": "/ping",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
